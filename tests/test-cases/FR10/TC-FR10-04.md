# TC-FR10-04

## Requirement ID
FR10

## Module / Test type / Technique
Order State Machine / Functional / Equivalence Partitioning

## Test design source
test-design/EP-FR10.md

## Coverage
| Coverage Type | Covered IDs |
|---|---|
| Equivalence Partitioning | FR10_EP_V04 |

## Detail
| ID | Test Objective | Token (Input) | Order ID (Input) | State Transition (Input) | Expected Output |
|---|---|---|---|---|---|
| FR10_EP_V04 | Gộp: FR10_EP_V04, FR10_EP_V06<br>- Admin cập nhật trạng thái đơn hàng `confirmed` -> `canceled` trong miền ST4<br>- Cập nhật trạng thái thành công trong DB và trả về phản hồi HTTP 200 trong miền O1 | Admin Token (TOK_A1) | Đơn hàng tồn tại (ORD1) | confirmed -> canceled (ST4) | - Cập nhật thành công, trạng thái mới là `canceled` (O1)<br>- Trả về HTTP 200 OK |

## Preconditions
1. Người dùng đăng nhập hệ thống với tài khoản Admin (`role = 'admin'`).
2. Tồn tại một đơn hàng ở trạng thái `confirmed` trong hệ thống.
3. Người dùng đang ở màn hình Quản lý đơn hàng (Admin).

## Test data
| Field | Value |
|---|---|
| Token JWT | Admin Token hợp lệ (TOK_A1) |
| Order ID | Đơn hàng tồn tại (ORD1) |
| Cặp chuyển đổi | confirmed -> canceled (ST4) |

## Test steps
1. Tìm đơn hàng có trạng thái `confirmed` trong danh sách.
2. Chọn đơn hàng đó và cập nhật trạng thái sang `canceled`.
3. Xác nhận lưu thay đổi.

## Expected results
1. Trạng thái đơn hàng được cập nhật thành công thành `canceled` trên giao diện Admin.
2. Trạng thái đơn hàng được cập nhật thành công thành `canceled` trong cơ sở dữ liệu.
3. API trả về phản hồi thành công (HTTP 200 OK).

## Actual results
1. Thành công: Trạng thái đơn hàng được cập nhật thành `canceled` trên giao diện Admin và trong cơ sở dữ liệu.
2. Thành công: API trả về phản hồi thành công (HTTP 200 OK).

## Status
Passed
