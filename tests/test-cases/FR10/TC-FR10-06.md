# TC-FR10-06

## Requirement ID
FR10

## Module / Test type / Technique
Order State Machine / Functional / Equivalence Partitioning

## Test design source
test-design/EP-FR10.md

## Coverage
| Coverage Type | Covered IDs |
|---|---|
| Equivalence Partitioning | FR10_EP_V06 |

## Detail
| ID | Test Objective | Token (Input) | Order ID (Input) | Current State (Input) | Expected Output |
|---|---|---|---|---|---|
| FR10_EP_V06 | Gộp: FR10_EP_V07, FR10_EP_V09<br>- Người dùng hủy đơn hàng của chính mình ở trạng thái `pending` trong miền CS_U1<br>- Trạng thái đơn hàng chuyển thành `canceled` trong DB và trả về HTTP 200 trong miền O_U1 | User Token (TOK_U1) | Đơn hàng của User (ORD_U1) | pending (CS_U1) | - Hủy thành công, trạng thái mới là `canceled` (O_U1)<br>- Trả về HTTP 200 OK |

## Preconditions
1. Người dùng đăng nhập hệ thống với tài khoản User thường.
2. Tồn tại đơn hàng ở trạng thái `pending` do chính User này đặt.
3. Người dùng đang ở trang Lịch sử đơn hàng hoặc Chi tiết đơn hàng.

## Test data
| Field | Value |
|---|---|
| Token JWT | User Token hợp lệ (TOK_U1) |
| Order ID | Đơn hàng của chính mình (ORD_U1) |
| Trạng thái hiện tại | pending (CS_U1) |

## Test steps
1. Chọn đơn hàng có trạng thái `pending` trong danh sách đơn hàng cá nhân.
2. Nhấp chọn nút "Hủy đơn hàng".
3. Xác nhận hủy trên hộp thoại xác nhận.

## Expected results
1. Hệ thống thực hiện hủy đơn hàng thành công, hiển thị thông báo thành công phù hợp.
2. Trạng thái đơn hàng đổi thành `canceled` trên giao diện của người dùng.
3. Trạng thái đơn hàng được cập nhật thành `canceled` trong cơ sở dữ liệu.
4. API trả về phản hồi thành công (HTTP 200 OK).

## Actual results
1. Thành công: Hệ thống thực hiện hủy đơn hàng thành công, hiển thị thông báo thành công.
2. Trạng thái đơn hàng đổi thành `canceled` trên giao diện của người dùng và trong cơ sở dữ liệu.
3. API trả về phản hồi thành công (HTTP 200 OK).

## Status
Passed
